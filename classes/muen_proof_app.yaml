inherit: [muen_proof, muen_component_cspecs]
buildVars: [COMPONENT_NAME, GPR_PATH]
buildSetup: |
  export GPR_FILE=$1/${GPR_PATH}
  export MUEN_BUILD_MODE=prove
  export OBJ_DIR=$(pwd)/obj/
  export EXEC_DIR=$(pwd)/bin/
buildScript: |
  muenCspecs ${COMPONENT_NAME} "${XML_PATH:-}"
  muenProof
