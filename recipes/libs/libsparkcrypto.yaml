multiPackage:
  dev:
    depends:
      - name: ${LIBSPARKCRYPTO_PROVIDER:-libs::libsparkcrypto_muen}-dev
        use: []

  test:
    depends:
      - name: ${LIBSPARKCRYPTO_PROVIDER:-libs::libsparkcrypto_muen}-test
        use: []
  proof:
    depends:
      - name: ${LIBSPARKCRYPTO_PROVIDER:-libs::libsparkcrypto_muen}-proof
        use: []
provideDeps: ['*']
